#include<stdio.h>
#include<string.h>

int main() {
	int n, k;
	char c = 'a';

	scanf("%d %d", &n, &k);
	for (int i = 1; i <= n; i++) {
		printf("%c", c);
		c++;
		if (i%k == 0) {
			c = 'a';
		}
	}

	return 0;
}

